// Settings app rendered with the Dust component library.
// Root contract: #PhoneNumberLabel (the theme picker is appendInline'd by Java).
$D = "../DustLib.ui";

Group {
  LayoutMode: Top;

  $D.@DustShellHeader #SettingsHeader { @Title = "Settings"; }

  Group #SettingsPanel {
    LayoutMode: Top;
    Background: $D.@DustPanelBackground;
    Padding: (Full: $D.@DustPanelPadding);

    Group #DeviceNumberSection {
      LayoutMode: Top;

      Label {
        Text: "Phone Number";
        Anchor: (Bottom: 6);
        Style: (FontSize: 12, TextColor: $D.@DustTextSecondary);
      }
      Label #PhoneNumberLabel {
        Text: "---";
        Style: (FontSize: 18, RenderBold: true, TextColor: $D.@DustTextPrimary);
      }
    }
  }
}
